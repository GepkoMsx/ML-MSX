;; CALL SetVramWrite


;; HL = address. (make sure right 16k vram page is selected)
SetVramWrite:
    push af
    ld   a, l
    out  ($99), a
    ld   a, h
    and  $3F
    or   $40                 ; Write bit
    out  ($99), a
    pop  af
    ret